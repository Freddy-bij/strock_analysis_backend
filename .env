# Database Configuration
# MongoDB Atlas (replace xxxxx with your actual cluster name)
# MONGODB_URI=mongodb+srv://friddybijanja31_db_user:IDxt0jZ84Wc60aaJ@stroke-analysis-cluster.xxxxx.mongodb.net/stroke_analysis?retryWrites=true&w=majority
# Fallback to local MongoDB if Atlas doesn't work
MONGODB_URI=mongodb://localhost:27017/stroke_analysis
# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# Server Configuration
PORT=8080

# CORS Configuration (if needed)
CORS_ORIGIN=http://localhost:3000
